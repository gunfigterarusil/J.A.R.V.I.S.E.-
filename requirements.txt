fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
pydantic>=2.0
python-dotenv>=1.0
httpx>=0.27
openai>=1.0
anthropic>=0.34
google-generativeai>=0.7

# Voice MVP
sounddevice
numpy
faster-whisper
pyttsx3

# Phase 3A — Acoustic Wake Word Detection (free, local, CPU-only)
# Enables "Hey Jarvis" style always-on detection before STT runs.
# 1. Install: pip install openwakeword
# 2. Download model: python -c "from openwakeword.utils import download_models; download_models(['hey_jarvis'])"
# 3. Enable in .env: VOICE_WAKE_WORD_DETECTOR=openwakeword  VOICE_WAKE_WORD_MODEL=hey_jarvis
#    pip install openwakeword

# High-quality neural TTS — Phase 4 (install whichever you want to use)
# Option A: XTTS v2 — local, voice cloning, Ukrainian support (~1.8 GB model auto-downloaded)
#   pip install TTS
# Option B: ElevenLabs — cloud, streaming, ~400ms latency (requires API key)
#   pip install elevenlabs>=1.0
# Note: sounddevice (already above) is required for both options

# V3/V9.6 Screen Reading + GUI Understanding MVP
mss
Pillow
pytesseract
pygetwindow

# Optional: build native desktop executable
pyinstaller

# V9.7 Safe GUI Automation MVP
pyautogui
psutil

# V15 optional desktop shell enhancements
pystray
plyer

# V18 modern desktop UI
customtkinter>=5.2
